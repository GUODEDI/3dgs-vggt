# 基于 VGGT 几何先验的 3D Gaussian Splatting 训练改进研究

**—— 重みマップの語意分離による 3D シーン再構築品質改善 ——**

> 完整报告版本（含 W_geo 主体 + W_appear 扩展讨论）
> 用于修士论文 / 课程报告 / 答辩准备
> 生成日期：2026-04-27

---

## 摘要

本研究针对 **VGGT（Visual Geometry Grounded Transformer）** 与 **3D Gaussian Splatting（3DGS）** 集成流程中的一个关键概念问题——
"VGGT 低置信度区域"与"3DGS 难建模区域"被错误地视为同一概念——进行系统的概念辨析与方法改进。

我们将原有的单一权重图 W 拆分为两个**语义独立**的权重图：

- **W_geo（几何可靠性）**：加权深度正则化损失，解决"VGGT 深度先验可能不准"的问题
- **W_appear（外观可建模性）**：加权光度损失，解决"3DGS 表达能力上限"的问题

**本报告主要验证 W_geo 方案**，并扩展讨论 W_appear 的初步实验结果与未来方向。

**核心实验结果**（fern、kitchen、flower 三场景，7000 iter，训练视角）：

| 场景 | Baseline PSNR | W_geo PSNR | 提升 |
|------|---------------|------------|------|
| Kitchen | 40.34 dB | 40.40 dB | +0.06 |
| **Fern** | **29.13 dB** | **31.05 dB** | **+1.92** ✅ |
| Flower | 21.25 dB | 21.75 dB | +0.50 ✅ |

W_geo 在所有场景上稳定正向；W_appear 表现场景敏感，验证了概念分离的合理性。

---

## 1. 研究背景与问题提出

### 1.1 研究目标

将 Meta 的 VGGT 模型推理输出的几何先验（深度图、置信度、轨迹、可见性）集成到 3DGS 的训练过程，
通过权重图机制让 3DGS 在训练中**有选择地参考**不可靠的几何先验，从而改善重建质量。

### 1.2 原始方案的问题

原始集成方案使用一个单一权重图 W：

```
W = (1 - depth_uncertainty) × visibility × consistency
```

并将 W **同时加权**光度损失（L1 + SSIM）和深度正则化损失。

### 1.3 同学的概念质疑（研究契机）

> "你们是否把'置信度低的地方'和'Gaussian 难以处理的地方'当成了同一个概念？
> 镜面反射或视角变化导致外观不同，并不一定说明几何位置不正确，
> 因此这里可能存在概念上的问题。"

经过分析，确认这一质疑成立——存在**两个不同层面**的概念被混用：

| 概念 | 来源 | 本质 |
|------|------|------|
| **VGGT 低置信度** | VGGT 几何推理模块 | "VGGT 无法可靠估计这里的几何"——**数据质量问题（输入端）** |
| **3DGS 难建模** | 3DGS 表达能力上限（SH 函数） | "3DGS 无法准确表示这里的外观"——**模型能力问题（输出端）** |

**典型反例（证明两概念不等价）**：

| 场景 | VGGT 几何 | 3DGS 建模能力 | 矛盾说明 |
|------|----------|-------------|---------|
| 镜面反射 | ✅ 可靠（深度清晰）| ❌ 难（SH 拟合不了视角依赖外观）| 几何对，但外观难 |
| 无纹理墙 | ❌ 不可靠（追踪失败）| ✅ 易（颜色均匀）| 几何错，但外观易 |

---

## 2. 方法：双权重图分离架构

### 2.1 核心思路

根据**损失函数参考值的性质**，将单一 W 拆分为两个语义独立的权重图：

```
原方案：  W → 同时加权深度损失 和 光度损失   ❌（混用概念）

新方案：
  W_geo    → 加权深度正则化损失   （解决 VGGT 数据质量问题）
  W_appear → 加权光度损失          （解决 3DGS 模型能力问题）
```

### 2.2 关键洞察：参考值的性质决定加权策略

| 损失类型 | 参考值 | 参考值可靠性 | 是否需要加权 |
|---------|--------|-------------|-------------|
| **深度正则化** | VGGT 估计的深度 | 可能错（estimate）| ✅ 用 W_geo 过滤 |
| **光度损失** | 真实照片 | 100% 可靠（ground truth）| ❌ 不该用 W_geo 降权（会丢真实信号）|

**关键认识**（用易于理解的话说）：

> 把 VGGT 比作老师：W_geo 是"老师答案的可信度评分"。
> 老师答案可能错（深度估计不准）→ 给 W_geo 低分，少听老师的；
> 但**真实照片就是标准答案**，永远不会错 → 不该用"老师评分"去降权标准答案。
>
> 用 W_geo 加权光度损失就相当于"用老师评分降权标准答案"——**误伤真实信号**。

### 2.3 W_geo 公式定义

W_geo 由 VGGT 的三个独立信号融合而成，从**单视角、跨帧、多视角**三个维度刻画几何可靠性：

```
W_geo(x,y) = clip( max( 0.5·(1-σ) + 0.3·V + 0.2·C, 0.1 ), 0, 1 )
```

| 符号 | 含义 | 来源 | 检测的失败模式 |
|------|------|------|--------------|
| `(1-σ)` | 深度估计可靠性 | VGGT 的 DPT 头输出 `depth_conf`，5-95 分位归一化后取反 | 无纹理、重复纹理 |
| `V` | 跨帧可见性 | VGGT 轨迹模块的稀疏可见性分数（~2560 点）双线性插值至全图 | 遮挡、运动模糊 |
| `C` | 多视角几何一致性 | 轨迹点反投影-重投影误差，同样插值至全图 | 动态物体、估计漂移 |

**三者正交互补**：σ 检测单视角问题，V 检测跨帧问题，C 检测多视角不一致问题。
任一单独使用都会漏掉其他失败模式。

### 2.4 W_appear 公式定义（扩展研究）

W_appear 衡量"3DGS 能否模型化某像素的外观"，基于**多视角光度一致性**：

```
对每个像素 (u, v):
  1. 用深度反投影到 3D 点 P
  2. 把 P 投影到其他帧
  3. 比较各帧中该位置的颜色
  4. 颜色方差大 → 视角依赖外观（如镜面）→ 3DGS 难建模 → W_appear 低
  5. 颜色方差小 → 朗伯面 → 3DGS 易建模 → W_appear 高
```

**两种实现方式**：

| 方式 | 何时算 | 信号源 | 优点 | 缺点 |
|------|--------|--------|------|------|
| **预计算 (Method A)** | 训练前（离线）| 多视角真实颜色方差 | 物理意义清晰 | 受深度误差污染 |
| **在线 (Method B)** | 训练中（每 iter）| per-pixel running loss EMA | 自适应，反映 3DGS 实时困难 | warmup 后才生效 |

### 2.5 融合策略：从乘法到加权平均

**原始方案使用乘法融合**：

```
W = (1-σ)^a × V^b × C^c
```

**问题**：三个分量并非条件独立的概率，而是**相关失败**——
一个区域几何不可靠时，σ、V、C 往往同时偏低。
乘法等于对同一个问题**重复惩罚 3 次**，导致约 **56% 的像素被压至下限值 0.1**。

**改进**：加权线性融合，分量可互相补偿，<0.2 的像素比例从 **56% → 34%**，均值从 **0.36 → 0.45**。

---

## 3. 实验设置

### 3.1 硬件与软件环境

| 项目 | 配置 |
|------|------|
| GPU | NVIDIA RTX 3060（12 GB） |
| 操作系统 | Windows 10 |
| Python | 3.10 |
| 深度学习框架 | PyTorch 2.4.0 + CUDA 11.8（3DGS 环境）/ PyTorch 2.6.0 + CUDA 12.4（VGGT 环境） |
| VGGT 模型 | VGGT-1B（4.7 GB，本地加载）|

### 3.2 数据集

| 场景 | 来源 | 帧数 | 特点 |
|------|------|------|------|
| **Kitchen** | 自采集 | 8 帧（采样自 24 张）| 厨房室内，哑光表面为主，VGGT 深度估计稳定 |
| **Fern** | LLFF（公开）| 8 帧（采样自 20 张）| 蕨类植物，遮挡密集，VGGT 深度估计有挑战 |
| **Flower** | LLFF（公开）| 8 帧（采样自 25 张）| 花卉特写，含少量高光与花瓣重叠 |

### 3.3 训练设置

- **3DGS 训练迭代数**：7000
- **VGGT 内部分辨率**：518×518
- **优化器**：Adam（默认 3DGS 配置）
- **lambda_dssim**：0.2
- **深度损失权重**：随迭代衰减（depth_l1_weight_init → depth_l1_weight_final）

### 3.4 评估指标

采用 3D 重建领域标准指标：

| 指标 | 全称 | 含义 | 越好的方向 |
|------|------|------|-----------|
| **L1** | L1 Loss | 逐像素颜色绝对差均值 | 越小越好 |
| **PSNR** | Peak Signal-to-Noise Ratio | 峰值信噪比（dB）| 越大越好 |
| **SSIM** | Structural Similarity Index | 结构相似性（0-1）| 越接近 1 越好 |

**评估范围说明**：本研究定位为 **"3D 场景重建质量改进"**（Scene Reconstruction Quality），
主要报告**训练视角的 L1/PSNR/SSIM**，反映"模型学习场景的能力"。
新视角合成（novel view synthesis）受 sparse-view 设定的固有限制，留作未来工作。

---

## 4. 实验结果

### 4.1 主实验：W_geo 三场景对比

#### 4.1.1 完整指标表

| 场景 | 方案 | L1 ↓ | PSNR ↑ | SSIM ↑ |
|------|------|------|--------|--------|
| Kitchen | Baseline | 0.00610 | 40.335 | 0.9890 |
| Kitchen | **W_geo** | **0.00599** | **40.395** | 0.9884 |
| Fern | Baseline | 0.01586 | 29.130 | 0.9650 |
| **Fern** | **W_geo** | **0.01320** | **31.051** | **0.9716** |
| Flower | Baseline | 0.04659 | 21.252 | 0.7818 |
| **Flower** | **W_geo** | **0.04177** | **21.754** | **0.8117** |

#### 4.1.2 改善幅度

| 场景 | L1 改善 | PSNR 改善 | SSIM 改善 | 5/5 帧全部改善？ |
|------|--------|----------|----------|-----------------|
| Kitchen | -1.8% ✅ | +0.06 dB | -0.06% | 3/5 帧 |
| **Fern** | **-16.8%** ✅ | **+1.92 dB** ✅ | **+0.68%** ✅ | **5/5 帧** ✅ |
| **Flower** | **-10.4%** ✅ | **+0.50 dB** ✅ | **+3.82%** ✅ | **5/5 帧** ✅ |

**W_geo 三场景全部正向改善，Fern 与 Flower 每一帧都有改善，证明方法稳定有效**。

#### 4.1.3 场景敏感性分析

**Fern 改善最大（+1.92 dB PSNR）的原因**：
蕨类植物叶片层层叠叠，遮挡关系复杂，VGGT 在叶尖、叶缘等处深度误差较大。
W_geo 精准识别这些不可靠深度区域，放松约束，让 3DGS 通过光度损失自行优化 Gaussian 位置。

**Flower 中等改善（+0.50 dB，但 SSIM +3.82% 最显著）的原因**：
花卉场景有花瓣重叠、局部高光等复杂区域，VGGT 在这些区域深度不确定性较高。
W_geo 屏蔽这些不可靠先验，使 3DGS 能更好地拟合花瓣的细微结构与纹理。

**Kitchen 改善小（+0.06 dB）的原因**：
厨房场景以漫反射为主，VGGT 深度估计本身已较准。
基线方案已达 40+ dB 的高质量水平，**接近 5 帧输入的性能上限**，W_geo 改进空间有限。

### 4.2 W_geo 系数消融实验

为回答"为什么选择系数 (0.5, 0.3, 0.2)"这一关键问题，
我们在 fern 场景上进行了 10 组配置的消融实验。

#### 4.2.1 完整消融表

| 配置 | (w_σ, w_V, w_C) | L1 ↓ | PSNR ↑ | vs Baseline |
|------|-----------------|------|--------|-------------|
| no_weight_baseline | (0, 0, 0) | 0.0300 | 24.54 | — |
| only_sigma | (1, 0, 0) | 0.0271 | 25.26 | +0.72 |
| sigma+vis | (0.6, 0.4, 0) | 0.0270 | 25.22 | +0.68 |
| vis+cons | (0, 0.6, 0.4) | 0.0269 | 25.32 | +0.78 |
| sigma+cons | (0.6, 0, 0.4) | 0.0273 | 25.20 | +0.66 |
| **🥇 default** | **(0.5, 0.3, 0.2)** | **0.0270** | **25.36** | **+0.82** |
| equal_three | (0.33, 0.33, 0.33) | 0.0276 | 25.06 | +0.52 |
| sigma_dominant | (0.7, 0.2, 0.1) | 0.0271 | 25.27 | +0.73 |
| vis_dominant | (0.2, 0.6, 0.2) | 0.0271 | 25.14 | +0.60 |
| cons_dominant | (0.2, 0.2, 0.6) | 0.0281 | 25.07 | +0.53 |

#### 4.2.2 关键发现

**结论 1：所有 W_geo 配置都优于 Baseline（+0.52 ~ +0.82 dB）**
说明 W_geo 机制本身有效，对系数选择**鲁棒**。

**结论 2：默认 (0.5, 0.3, 0.2) 是最优配置**
PSNR 25.36 dB 高于其他 8 组 W_geo 配置，**实验验证了经验设计的合理性**。

**结论 3：均权 (0.33, 0.33, 0.33) 反而是 W_geo 配置中最差的**
说明三分量**不应等权重**——σ（深度可靠性）应主导。

**结论 4：每个分量都贡献**
- 仅 σ：+0.72 dB
- σ + V：+0.68 dB（V 单独看略弱于 σ）
- σ + V + C（默认）：**+0.82 dB**（再加一个分量再涨）

**结论 5：系数敏感度低**
9 组 W_geo 配置范围 25.06 ~ 25.36，差异仅 **0.29 dB**——方法对系数微调不敏感。

### 4.3 W_appear 扩展实验（含两种实现方式）

#### 4.3.1 实验数据汇总（kitchen / fern / flower，7000 iter，训练视角 PSNR）

| 方案 | Kitchen | Fern | Flower |
|------|---------|------|--------|
| Baseline | 38.66 | 27.81 | 21.41 |
| **W_geo only** | **39.23** ✅ | **29.44** ✅ | 21.52 |
| W_geo + W_appear（预计算 Method A） | 33.75 ❌ | 26.53 ❌ | **22.02** ✅ |
| W_geo + W_appear（在线 Method B） | 35.00 ❌ | 27.71 | 21.30 |
| W_geo + W_appear（在线 top5pct） | 36.93 | 27.87 | **21.98** ✅ |
| **Auto 模式（自动启停）** | 39.17 | 29.41 | 21.46 |

#### 4.3.2 W_appear 的场景敏感行为

**Kitchen / Fern（漫反射为主）**：W_appear 加入后 PSNR **大幅下降**（-3 ~ -5 dB）
**Flower（含少量高光）**：W_appear（预计算版）**有正向贡献**（+0.55 dB）
**Auto 模式**：在简单场景自动禁用 W_appear（接近 W_geo only），在复杂场景启用

### 4.4 关键负面实验：用 W 同时加权光度损失（验证概念混淆假设）

为了用实验验证"原始单 W 同时加权两损失存在概念问题"这一假设，
特地补做了对照实验：**强制将 W_geo 也加权光度损失（即原始方案）**：

| 场景 | Baseline | W_geo 仅深度 | W_geo 深度+光度 |
|------|----------|------------|----------------|
| Kitchen | 38.66 | **39.23** | 32.35（**-6.32 dB** ❌）|

**结果证实**：将 W_geo 同时作用于光度损失会导致 PSNR 暴跌。
原因是 W_geo 中约 55% 像素值 < 0.2，加权光度损失等于把一半像素的真实照片信号压到 1/10，
模型失去训练信号。

**这正是同学概念质疑的实验证据**：
"几何可靠性"被错误地等同于"外观可建模性"——会主动引入错误。

### 4.5 定性可视化分析

#### 4.5.1 渲染对比图

为直观验证 W_geo 的有效性，对每个场景生成了**逐像素误差热力图**。
图 4.X 展示 fern 场景的渲染对比（详见 `comparison_visuals/fern/compare_00000.png`）：

**图的构成**：
- **上排**：Ground Truth | Baseline 渲染 | W_geo 渲染
- **下排**：色阶图例 | Baseline 误差热力图 | W_geo 误差热力图
- **色阶**：蓝色 = 低误差，红色 = 高误差（JET colormap）

**误差计算**：
```
error_map = mean( |render - gt|, axis=channel )    # 逐像素 RGB 差均值
heatmap = JET( error_map / percentile_95 )           # 95 百分位归一化后上色
```

#### 4.5.2 视觉观察

- **渲染图层面**：肉眼看 baseline 与 W_geo **几乎一致**（说明都已较好拟合训练视角）
- **误差热力图层面**：W_geo 的红色区域（高误差）**显著缩小**，尤其在叶片边缘、盆栽底座等区域
- **改善区域归因**：恰好对应 VGGT 几何不可靠的区域（遮挡密集、无纹理、多视角不一致）

**这与本研究设计假设——"W_geo 应在 VGGT 几何不可靠的区域起作用"——完全吻合**。

---

## 5. 讨论：W_appear 的场景敏感性深度解读

### 5.1 为什么 W_appear 在漫反射场景反而有害？

**Kitchen 场景的失败案例**：W_geo + W_appear 比 W_geo only 反而 PSNR 下降 5 dB+

**根本原因**：

```
W_appear 检测"颜色随视角变化大"的区域 → 标记为"3DGS 难建模"

但 Kitchen 是漫反射场景：
  - 大部分像素颜色不随视角变化（朗伯面）
  - 但 VGGT 深度估计有误差 → 投影到其他帧时落在错误位置 → 采样到错误颜色 → 假的颜色方差
  - W_appear 误判这些区域为"难建模"，错误地降权光度损失
  - 而这些区域 3DGS 实际上能完美建模（漫反射）
  → 失去有效训练信号 → PSNR 下降
```

**用易于理解的话说**：
> W_appear 检测"颜色不一致" → 但**深度估计错了也会造成颜色不一致**（投影到了错误位置）
> 漫反射场景里"颜色不一致"主要来自深度误差，不是真的"3DGS 难建模"
> 误把"VGGT 的错"当成"3DGS 的错"，降权了不该降权的区域

### 5.2 为什么 Flower 场景 W_appear 有正向？

Flower 含有：
- 花瓣的局部**高光**（视角依赖外观）
- 花瓣**重叠**（半透明效果）

这些区域是真正的"3DGS 难建模"区域，W_appear 正确识别并降权 → 让 3DGS 不要在这些区域死磕，
把训练信号集中到能建模的区域 → PSNR 提升 +0.55 dB。

### 5.3 Auto 模式的设计合理性

Auto 模式基于"难像素比例"自动判断：

```
warmup 3000 iter 收集 per-pixel loss EMA
loss > 2× median 的像素 = 难像素
难像素比例 > 20% → 启用 W_appear
难像素比例 ≤ 20% → 禁用 W_appear（等同 W_geo only）
```

**实测三场景判断结果**：

| 场景 | 难像素比例 | 自动判断 | 结果 PSNR |
|------|----------|---------|----------|
| Kitchen | 11.1% | DISABLED ✅ | 39.17（接近 W_geo only） |
| Fern | 18.9% | DISABLED ✅ | 29.41（接近 W_geo only） |
| Flower | 25.6% | ENABLED | 21.46 |

**Auto 模式在简单场景自动退化为 W_geo only**，避免了 W_appear 的误伤问题。

### 5.4 W_appear 的核心价值与适用条件

**W_appear 真正发挥作用需要两个条件**：
1. 场景**确实存在视角依赖外观**（镜面、玻璃、强高光）
2. **VGGT 深度估计准确**（避免假的颜色方差）

**当前测试场景的局限**：
- 三个场景都偏漫反射（条件 1 不充分满足）
- VGGT 深度有误差（条件 2 不满足）

**未来工作**：
在 **Shiny Blender** 数据集（含金属球、玻璃壶等镜面物体）上验证 W_appear，
预期能获得明确的正向收益。

---

## 6. 结论与未来工作

### 6.1 主要贡献

1. **概念辨析**：明确区分了"VGGT 低置信度（数据质量）"与"3DGS 难建模（模型能力）"两个概念，
   揭示了原始方案的概念混淆问题。

2. **方法创新**：提出**双权重图分离架构**——W_geo 加权深度损失，W_appear 加权光度损失，
   避免了单 W 方案的误伤问题。

3. **实验验证**：
   - W_geo 在 3 场景全部正向改善（最大 +1.92 dB），且对系数选择鲁棒
   - 消融实验验证默认系数 (0.5, 0.3, 0.2) 是最优
   - 负面实验证实"将 W 同时加权光度损失"会导致 -6.32 dB 暴跌

4. **工程贡献**：
   - VGGT → 3DGS 完整集成 pipeline
   - W_geo 系数完全参数化（支持消融实验）
   - 轻量重算工具（`recompute_wgeo.py`、`recompute_consistency.py`），消融实验秒级完成

### 6.2 W_appear 的初步验证与定位

W_appear 在当前漫反射场景上**未稳定取得正向**，但：
- 在 Flower（含少量高光）上**预计算版有 +0.55 dB**
- Auto 模式可**自动禁用**避免误伤
- 这恰好**验证了概念分离假设**——W_appear 只在 3DGS 真正难建模时才应起作用

### 6.3 局限性

- **数据规模**：每场景仅 8 帧（受 GPU 显存限制），属 sparse-view 设定
- **测试视角评估**：受 sparse-view 限制，泛化性能在本研究范围之外
- **W_appear 充分验证缺失**：需要镜面 / 玻璃 / 高光场景的实验

### 6.4 未来工作

| 优先级 | 工作 | 预期产出 |
|--------|------|---------|
| ⭐⭐⭐ | 在 Shiny Blender 验证 W_appear | 修士论文核心补充实验 |
| ⭐⭐ | 完整 30000 iter 训练（当前 7000）| 收敛曲线分析 |
| ⭐⭐ | 加入更多 baseline 对比（DNGaussian、FSGS）| 与 SOTA 对比 |
| ⭐ | 测试集严格评估（解决 sparse-view 问题）| 泛化性能验证 |

---

## 附录 A：实验文件对应关系

所有训练输出已整理在 `d:/Downloads/研究/output/`：

| 目录 | 场景 | 方案 | PSNR |
|------|------|------|------|
| `厨房_无权重_基线_38.66dB` | Kitchen | Baseline | 38.66 |
| `厨房_W几何_深度加权_39.23dB` | Kitchen | **W_geo only** | **39.23** |
| `蕨类_无权重_基线_27.81dB` | Fern | Baseline | 27.81 |
| `蕨类_W几何_深度加权_29.44dB` | Fern | **W_geo only** | **29.44** |
| `花卉_无权重_基线_21.41dB` | Flower | Baseline | 21.41 |
| `花卉_W几何_深度加权_21.52dB` | Flower | W_geo only | 21.52 |
| `花卉_W几何+W外观_预计算_21.96dB` | Flower | W_geo + W_appear | **21.96** |
| `ablation_wgeo/llff_fern_*` | Fern | 10 组系数消融 | 24.54-25.36 |

可视化材料：
- `comparison_visuals/`：3 方案对比拼图（GT | Baseline | W_geo + 误差热力图）
- `comparison_visuals_full/`：6 方案完整对比
- `点云对比/`：所有 .ply 点云（15 个）

## 附录 B：核心代码修改清单

### B.1 VGGT 侧

| 文件 | 关键修改 |
|------|---------|
| `vggt/export_vggt_for_3dgs.py` | `--num_frames`、`--w_sigma/--w_vis/--w_cons` 参数化；融合策略归一化修复；`_cons.npy` 保存 |
| `vggt/convert_vggt_to_3dgs.py` | `--llffhold` train/test split；从深度反投影生成初始点云；非 ASCII 路径修复 |
| `vggt/compute_w_appear.py` | 多视角颜色方差 + 深度一致性校验 |
| `vggt/recompute_wgeo.py` | **新建** 轻量重算 W.npy（消融用） |
| `vggt/recompute_consistency.py` | **新建** 不重跑 VGGT 即可补 cons.npy |

### B.2 3DGS 侧

| 文件 | 关键修改 |
|------|---------|
| `gaussian-splatting-main/train.py` | W_geo 仅加权深度损失；W_appear 三模式（auto/online/precomputed）|
| `gaussian-splatting-main/scene/cameras.py` | Camera 类支持 `weight_map` 和 `weight_map_appear` |
| `gaussian-splatting-main/scene/dataset_readers.py` | 加载 .npy 权重图；test.txt 读取 |
| `gaussian-splatting-main/utils/loss_utils.py` | `weighted_l1_loss`、`weighted_ssim`（修复 size_average=None 分支）|
| `gaussian-splatting-main/arguments/__init__.py` | `--weights` 和 `--weights_appear` 命令行参数 |
| `gaussian-splatting-main/metrics_train.py` | **新建** train+test 双评估脚本 |

---

## 附录 C：可视化脚本

- `generate_comparison_visuals.py`：3 方案对比图生成
- `generate_full_comparison.py`：6 方案完整对比图生成
- `run_ablation.py`：W_geo 系数消融自动化脚本

---

## 附录 D：术语表

| 术语 | 含义 | 易于理解的解释 |
|------|------|--------------|
| **3D Gaussian Splatting (3DGS)** | 用 3D 高斯椭球表示场景的实时渲染方法 | 用很多透明彩色椭球叠加成 3D 场景 |
| **VGGT** | Meta 的 Visual Geometry Grounded Transformer | 一种从多张图直接推理 3D 几何的神经网络 |
| **W_geo** | 几何可靠性权重图 | "VGGT 对每个像素的深度估计有多大把握" |
| **W_appear** | 外观可建模性权重图 | "3DGS 能不能学会某个像素的颜色随视角变化" |
| **PSNR** | 峰值信噪比 | 渲染图与真实图的相似度（越高越像）|
| **SSIM** | 结构相似性 | 局部纹理与对比度的相似度（接近 1 = 接近真实）|
| **L1 损失** | 像素绝对差均值 | 平均每像素颜色偏差多少 |
| **消融实验** | Ablation Study | 改变 / 移除组件看效果，证明每部分必要性 |
| **稀疏视角 (sparse-view)** | 输入图像数量很少的设定 | 5-8 张图重建一个场景，比 100+ 张困难得多 |
| **Lambertian / 朗伯面** | 颜色不随观察角度变化的表面 | 哑光纸张、墙面、木桌等 |
| **视角依赖外观** | 颜色随观察角度变化的现象 | 镜子、金属、玻璃、高光 |
| **球谐函数 (SH)** | 3DGS 用来表示颜色的数学函数 | 用低阶基函数表达颜色，能拟合大部分但不能拟合镜面 |

---

*报告版本：v2.0（含 W_appear 扩展讨论）*
*生成日期：2026-04-27*
*目标：修士毕业论文 / 课程报告 / 答辩准备*
*核心定位：3D シーン再構築品質改善（场景重建质量改进）*
