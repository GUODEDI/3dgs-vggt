# W_geo 和 W_appear 说明

> 本研究提出的两个权重图：是什么、含义、作用

---

## 一、总览

| | W_geo | W_appear |
|---|---|---|
| **是什么** | 几何可靠性权重图 | 外观可表达性权重图 |
| **衡量什么** | VGGT 对某像素的几何估计有多可靠 | 3DGS 能否建模某像素的外观 |
| **关注的失败模式** | VGGT 输入端的不确定性 | 3DGS 输出端的表达能力上限 |
| **作用于哪个损失** | 深度正则化损失 | 光度损失 |
| **为什么需要加权** | 参考值（VGGT 深度）可能错 → 需过滤 | 参考值（真实照片）100% 对，但要标记 3DGS 难表达的区域 |

**核心原则**：不同的失败模式，用不同的权重图，且**只作用于对应的损失项**。

---

## 二、W_geo —— 几何可靠性权重图

### 1. 是什么

W_geo 是一张和图像同尺寸的权重图，每个像素一个 [0, 1] 的值，表示 **VGGT 对该像素的几何估计有多可靠**。

### 2. 含义

```
W_geo 高 → VGGT 在这里几何估得准 → 可以信
W_geo 低 → VGGT 在这里几何不靠谱 → 不能信
```

**典型场景**：

| 像素位置 | W_geo | 原因 |
|---|---|---|
| 纹理清晰的物体边缘 | 高 ≈ 0.9 | 形状特征明显，深度好估 |
| 白墙、均匀天花板 | 低 ≈ 0.1 | 没有特征点，深度估不准 |
| 被遮挡的区域 | 低 | 跨帧追踪失败 |
| 动态物体 | 低 | 多视角不一致 |

### 3. 公式（三分量加权融合）

```
W_geo = 0.5 · (1 - σ) + 0.3 · V + 0.2 · C
```

| 分量 | 含义 | 捕捉的失败模式 |
|---|---|---|
| **(1 - σ)** | 深度可靠性（VGGT 深度头置信度） | 单视角下的估计失败（无纹理、重复模式） |
| **V** | 跨帧可见性（VGGT 轨迹模块） | 遮挡、运动模糊、出画面 |
| **C** | 多视角几何一致性（反投影误差） | 动态物体、估计漂移、镜面虚像 |

三者**正交互补**，单独用一个会漏掉其他失败模式。

### 4. 作用

**只作用于深度正则化损失**：

```
L_depth = | 渲染深度 - VGGT 深度 | × W_geo
```

**为什么这样作用**：
- 深度损失的参考值是 **VGGT 的估计深度** → 可能错 → 必须用 W_geo 过滤掉错的地方，否则 3DGS 会被带偏
- VGGT 错的地方，W_geo 自动变低，那里的深度损失被降权，3DGS 就不会去硬学错的深度

---

## 三、W_appear —— 外观可表达性权重图

### 1. 是什么

W_appear 也是一张和图像同尺寸的权重图，每个像素一个 [0, 1] 的值，表示 **3DGS 能否用 SH 函数建模该像素的外观**。

### 2. 含义

```
W_appear 高 → 该像素是漫反射（朗伯）面 → 3DGS 容易建模
W_appear 低 → 该像素视角依赖性强（镜面/高光）→ 3DGS 难建模
```

**典型场景**：

| 像素位置 | W_appear | 原因 |
|---|---|---|
| 哑光的木桌、布料、墙面 | 高 ≈ 0.9 | 朗伯面，多视角颜色一致 |
| 金属球、光亮地板 | 低 ≈ 0.2 | 镜面反射，颜色随视角变 |
| 玻璃、水面 | 低 | 折射 + 反射，3DGS 表达不了 |

### 3. 信号来源

**多视角光度一致性**：把同一个 3D 点投影到多帧上，看颜色是否一致。

```
颜色一致     → 朗伯面     → 3DGS 易建模 → W_appear 高
颜色不一致   → 视角依赖   → 3DGS 难建模 → W_appear 低
```

### 4. 作用

**只作用于光度损失**：

```
L_photo = | 渲染图像 - 真实图像 | × W_appear
```

**为什么这样作用**：
- 光度损失的参考值是 **真实照片** → 100% 正确 → 不需要"过滤错值"
- 但 3DGS 在镜面区域**根本表达不了**那个颜色，硬逼它去拟合 → 会损坏其他区域的高斯参数
- W_appear 在镜面处变低 → 那里的光度损失被降权 → 3DGS 不必为表达不了的东西硬塞参数

---

## 四、为什么必须分成两个

### 同学的质疑（本研究的起点）

> "VGGT 置信度低的地方" ≠ "Gaussian 难处理的地方"

### 两个反例证明它们正交

| 反例 | VGGT 置信度 | 3DGS 建模能力 |
|---|---|---|
| **镜面反射** | **高**（形状清楚） | **低**（SH 表达不了） |
| **白墙** | **低**（无特征） | **高**（颜色均匀） |

→ 完全相反 → 必须用**两张独立的权重图**分别对待。

### 加权策略的根本原则

| 损失 | 参考值 | 性质 | 用什么过滤 |
|---|---|---|---|
| 深度正则化损失 | VGGT 估计深度 | 可能错 | **W_geo** |
| 光度损失 | 真实照片 | 一定对，但 3DGS 不一定能拟合 | **W_appear** |

**关键设计原则**：**权重图只作用于其对应失败模式所属的损失项**，绝不交叉。

---

## 五、一句话总结

> **W_geo 衡量「VGGT 在这里靠不靠谱」，只过滤深度损失。**
> **W_appear 衡量「3DGS 在这里能不能表达」，只过滤光度损失。**
> **两者对应不同失败模式，必须独立、绝不交叉。**

---

*日期：2026-04-28*
