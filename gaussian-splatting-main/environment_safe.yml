name: gaussian_splatting
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - cudatoolkit=11.6
  - plyfile
  - python=3.7.13
  - pytorch=1.12.1
  - torchaudio=0.12.1
  - torchvision=0.13.1
  - tqdm
  - pip:
    - opencv-pytho
    
    - joblib
    # CUDA 扩展稍后手动安装，避免安装时出错
    # - submodules/diff-gaussian-rasterization
    # - submodules/simple-knn
    # - submodules/fused-ssim

