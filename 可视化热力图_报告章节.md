# 可视化对比图（Visualization） — 报告章节

> 用于修士论文 / 课程报告 / PPT 的视觉对比图说明文字
> 配图：`comparison_visuals/fern/compare_00000.png`

---

## 4.X 定性可视化分析（Qualitative Visualization）

### 4.X.1 图示说明

为了直观验证 W_geo 方案的有效性，我们对每个场景的渲染结果进行了**逐像素误差可视化**。
图 4.X 展示了 fern 场景的对比结果。

**图的构成（六宫格 + 色阶图例）：**

| | 上排（渲染图） | 下排（误差热力图） |
|--|--------------|------------------|
| 左 | **Ground Truth**（真实照片，对比基准）| 误差色阶图例 |
| 中 | **Baseline 渲染图**（无 W_geo） | **Baseline 误差热力图** |
| 右 | **W_geo 渲染图** | **W_geo 误差热力图** |

**色阶规则**（误差热力图）：

```
蓝色  ←  低误差（渲染准确）
青色
黄色
橙色
红色  ←  高误差（渲染失真）
```

误差定义为渲染图与真实照片在每个像素位置的 RGB 绝对差均值（与 L1 损失同义），
经 95 百分位归一化后通过 **JET colormap** 上色。

### 4.X.2 定量指标对比

图中标注的 L1 数值：

| 方法 | L1 误差 | 相对降幅 |
|------|--------|---------|
| Baseline | 4.1 | — |
| **W_geo** | **3.2** | **-22%** ✅ |

### 4.X.3 视觉观察与分析

#### (1) 渲染图层面（上排）

肉眼直接观察 Baseline 与 W_geo 的渲染图，**整体场景结构均已较好恢复**：

- 棕榈叶片的形状、纹理基本可见
- 背景的玻璃柜、墙面、地面也得到重建
- 两种方法在**全局视觉**上差异不显著（这是因为基础 3DGS 已具备较强的拟合能力）

**关键洞察**：单看渲染图无法明显区分两种方法的优劣，
这正说明了误差热力图作为**严谨可视化工具**的必要性。

#### (2) 误差热力图层面（下排）

通过逐像素误差可视化，差异立即变得清晰：

**Baseline 误差热力图**（中下）：
- 棕榈叶片的**叶尖、叶缘**呈现成片橙色至红色高误差区域
- 盆栽底座、地面、左侧墙面也存在多处红色噪声斑块
- 整体红色区域面积较大，反映出 Gaussian 在这些位置出现明显偏差

**W_geo 误差热力图**（右下）：
- 同一棕榈叶片的高误差区域**显著缩小**，更多变为蓝色（低误差）
- 叶片边缘的红色噪声明显减少
- 盆栽底座区域的橙色斑块也变浅
- 全图蓝色面积增大，整体误差水平下降

#### (3) 改善区域归因

W_geo 改善最明显的区域，恰好对应 VGGT **几何不可靠的区域**：

| 改善区域 | VGGT 几何状态 | W_geo 起到的作用 |
|---------|--------------|----------------|
| 叶片边缘 | 遮挡密集，深度跳变 | 放松不可靠深度约束 |
| 盆栽底座 | 单视角无纹理 | 降低低置信度区域权重 |
| 玻璃柜反光区 | 多视角不一致 | 抑制 consistency 低的像素 |

这与本研究的设计假设——**"W_geo 应在 VGGT 几何不可靠的区域起作用"**——完全吻合。

### 4.X.4 与定量指标的呼应

视觉观察与 L1 数值改善（**4.1 → 3.2，降低 22%**）一致：

- 视觉上**红色区域面积减少** → 数值上**误差均值下降**
- 视觉上**蓝色区域扩大** → 数值上**低误差像素占比提高**

这种**视觉观察与定量指标互为印证**的现象，
是科研中验证方法有效性的标准做法（Sanity Check）。

### 4.X.5 局限性讨论

**Kitchen 场景的视觉对比相对不显著**：
- Baseline 已达 40+ dB 高质量水平
- 两种方法的误差热力图差异不易肉眼分辨
- 这与定量指标"Kitchen 改善仅 +0.06 dB"的现象一致
- 反映 W_geo 在简单漫反射场景的改进空间有限

**Fern / Flower 场景视觉对比更清晰**：
- 因为这些场景 VGGT 几何估计存在明显误差
- W_geo 的"屏蔽不可靠几何"机制有充分发挥空间

---

## 附录：可视化生成方法（技术说明）

### 数据来源

- 渲染图：`gaussian-splatting-main/render.py` 输出，从训练相机视角逐张渲染
- 真实照片：3DGS 训练时加载的 ground truth（`train/ours_7000/gt/*.png`）

### 误差计算

```python
diff = abs(render - gt)             # 逐像素 RGB 差
error_map = diff.mean(axis=-1)       # RGB 三通道平均，得到 [H, W] 误差图
```

### 颜色映射

```python
p95 = np.percentile(error_map, 95)   # 用 95 百分位做归一化（避免极端值拉爆色阶）
norm = clip(error_map / p95, 0, 1)
heatmap = cv2.applyColorMap((norm * 255).astype(np.uint8), cv2.COLORMAP_JET)
```

### 生成脚本

`d:/Downloads/研究/generate_comparison_visuals.py`（核心 3 方案对比）
`d:/Downloads/研究/generate_full_comparison.py`（5 方案完整对比）

---

## 用于 PPT 的简化说明

如果 PPT 篇幅有限，可以用这段简短描述：

```
图 X：fern 场景渲染对比与误差热力图

上排：真实照片 / Baseline 渲染 / W_geo 渲染
下排：色阶图例 / Baseline 误差 / W_geo 误差

蓝色 = 低误差，红色 = 高误差。

观察：W_geo 误差图中红色区域明显减少（叶片边缘、盆栽底座），
对应数值改善 L1 4.1 → 3.2（-22%）。
```

---

## 用于日文修士论文的版本

```markdown
### 4.X 定性的可視化分析

提案手法の有効性を視覚的に検証するため、
各シーンのレンダリング結果に対して**ピクセル単位の誤差可視化**を行った。

**図の構成**：
- 上段：Ground Truth ｜ Baseline レンダリング ｜ W_geo レンダリング
- 下段：色階図例 ｜ Baseline 誤差ヒートマップ ｜ W_geo 誤差ヒートマップ
- 色階：青 = 低誤差、赤 = 高誤差（JET colormap）

**観察**：
- レンダリング画像のみでは Baseline と W_geo の差は微小
- しかし誤差ヒートマップでは差異が明確：
  W_geo 側の赤色領域（高誤差）が**棕櫚葉の縁**および
  **植木鉢の底座**で顕著に減少している
- これは定量指標（L1: 4.1 → 3.2、22% 減）と一致する

**改善が顕著な領域は VGGT の幾何推定が不確かな領域**
（遮蔽密集領域、無テクスチャ平面、多視点不一致領域）と一致しており、
W_geo の設計意図——「VGGT の不確実な幾何先験を抑制する」——が
正しく機能していることが視覚的にも確認できた。
```

---

*版本：v1.0*
*生成日期：2026-04-27*
*配图：comparison_visuals/fern/compare_00000.png*
