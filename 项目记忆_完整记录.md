# 3DGS-VGGT 项目记忆 / Project Memory

> 本文档记录 2026-04 期间 VGGT + 3DGS 集成项目的完整工作上下文，
> 包括项目背景、关键决策、实验结果、代码修改、未完成事项等。
> 用于跨会话 / 跨模型的工作交接。

---

## 0. 修士毕业论文（修士論文）信息

### 0.1 题目（2026-04-27 已确定）

✅ **最终题目**：
> 「VGGTの幾何的事前知識を用いた3D Gaussian Splattingの**学習改善**に関する研究」
> 副題：幾何的信頼性と外観可表現性の重みマップ分離
>
> *Research on Improving 3D Gaussian Splatting **Training** using Geometric Priors from VGGT*
> *— Weight Map Separation between Geometric Reliability and Appearance Modelability —*

### 0.1.5 研究定位（2026-04-27 重要决定）

**核心定位：「3D シーン再構築品質改善」（场景重建质量改进），非「新视角合成」**

**为什么这样定位**：
- 实验是 sparse-view 设定（5-8 帧），3DGS 在测试视角的泛化能力受 sparse-view 本身限制
- W_geo 改进的是**训练过程**：让 3DGS 更好地拟合到 VGGT 提供的几何先验
- Train PSNR/SSIM/LPIPS 反映"学习场景的能力"——这正是本研究的范围

**评估范围**：
- ✅ 主要指标：训练视角的 L1 / PSNR / SSIM（"学习场景的能力"）
- ⚠️ 次要参考：测试视角（仅作 sparse-view 限制的讨论，不作主指标）
- ❌ 不强调：novel view synthesis（不是本研究目标）

**研究边界（写在论文里）**：
> 本研究の主要貢献は VGGT の幾何先験を 3DGS 学習に正しく統合することであり、
> 訓練ビューでの再構築品質改善を主指標とする。
> Novel view synthesis（新視点合成）における汎化性能は sparse-view 設定の制約に依存し、
> 本研究の対象外（今後の課題）。

**这样定位的好处**：
1. 现有 Train PSNR 数据（38.66→39.23 等）直接可用，不用重跑
2. 故事自洽：W_geo 的本质就是"指导训练"，对应 train metrics 是合理的
3. 避免了 sparse-view 过拟合带来的"测试集 11 dB"尴尬数据
4. 修士答辩时可清晰回答"评估范围"问题

### 0.2 W_geo 系数消融实验结果（2026-04-27 已完成）

**实验设置**：fern 场景，8 帧，7000 iter，10 组配置

**完整结果**：

| 配置 | (w_σ, w_V, w_C) | L1 ↓ | PSNR ↑ | vs baseline |
|------|-----------------|------|--------|-------------|
| no_weight_baseline | (0, 0, 0) | 0.0300 | 24.54 | — |
| only_sigma | (1, 0, 0) | 0.0271 | 25.26 | +0.72 |
| sigma+vis | (0.6, 0.4, 0) | 0.0270 | 25.22 | +0.68 |
| vis+cons | (0, 0.6, 0.4) | 0.0269 | 25.32 | +0.78 |
| sigma+cons | (0.6, 0, 0.4) | 0.0273 | 25.20 | +0.66 |
| **🥇 default** | **(0.5, 0.3, 0.2)** | **0.0270** | **25.36** | **+0.82** |
| equal_three | (0.33, 0.33, 0.33) | 0.0276 | 25.06 | +0.52 |
| sigma_dominant | (0.7, 0.2, 0.1) | 0.0271 | 25.27 | +0.73 |
| vis_dominant | (0.2, 0.6, 0.2) | 0.0271 | 25.14 | +0.60 |
| cons_dominant | (0.2, 0.2, 0.6) | 0.0281 | 25.07 | +0.53 |

**关键结论**（论文用）：
1. ✅ **9 组 W_geo 配置全部优于 baseline**（+0.52 ~ +0.82 dB）
2. ✅ **默认 (0.5, 0.3, 0.2) 是最优**——证明经验设计合理
3. ✅ **均权 (0.33, 0.33, 0.33) 反而最差**——sigma 应主导
4. ✅ **三分量都贡献**：去掉任一个 PSNR 会下降
5. ✅ **配置间差异 0.29 dB**——方法对系数选择**鲁棒**

**用于答辩的话术**：
> 「W_geo 係数 (0.5, 0.3, 0.2) は経験的に決定したが、消融実験で検証済み：
> 9 組の係数構成を比較し、デフォルトが最良（+0.82 dB）。
> 各分量を取り除く実験では、3 つすべて貢献していることを確認。
> 均等重み (0.33, 0.33, 0.33) は最弱（+0.52 dB）で、深度信頼度が主導すべきことが分かる。
> 構成間の差は 0.29 dB と小さく、本手法のロバスト性も高い。」

### 0.3 Train/Test Split 实验发现（重要）

**数据**（kitchen, 8 帧训练 + 1 帧测试，7000 iter）：

| | Baseline | W_geo |
|--|----------|-------|
| **Train PSNR** | 37.15 dB | 37.62 dB（+0.47）|
| **Test PSNR** | **11.29 dB** | 10.98 dB（差异在噪声范围）|

**发现**：
- 训练 PSNR 和测试 PSNR 差距 **25 dB**——证明 sparse-view 设定下严重过拟合
- W_geo 对训练 PSNR 有帮助（+0.47 dB），但对测试 PSNR 无统计学意义的提升
- **测试集仅 1 帧，且是序列起点（外推视角）**，单帧统计极不稳定
- 这是 sparse-view 3DGS 的固有问题，与 W_geo 设计无关

**应对策略**（已采用）：研究重新定位为「3D 场景重建质量改进」，主要看 train 指标

### 0.1.x 其他备选题目（保留参考）

**候选 2 — 聚焦核心创新**
> 「3D Gaussian Splattingにおける幾何的信頼性と外観可表現性の分離による重みマップ設計」

**候选 3 — 从概念质疑切入**
> 「VGGTと3D Gaussian Splattingの統合における概念的混同の解決」
> 副題：二つの重みマップによる幾何先験の正しい活用

**候选 4 — Sparse-view 方向定位**
> 「少数視点入力に対するVGGT事前知識を用いた3D Gaussian Splatting学習の高精度化」

**候选 5 — 简洁版**
> 「VGGT事前知識を活用した重みマップ分離による3D Gaussian Splatting」

### 0.2 论文叙事主线

> "VGGT 的几何先验如何被 3DGS 正确使用？——一项关于权重图语义分离的研究"
>
> 1. 问题发现：现有 VGGT + 3DGS 集成方案存在概念混淆（"几何不可靠" ≠ "外观难建模"）
> 2. 提出方法：双权重图分离架构 W_geo + W_appear
> 3. 实验验证：漫反射 + 镜面场景对比
> 4. 失败案例：W_appear 在漫反射场景反而有害（恰好验证概念分离假设）
> 5. 结论：双权重图架构是 VGGT 类几何先验工作的合理使用方式

### 0.3 修士论文标准章节结构（日文）

1. **序章（はじめに）**：3D 重建背景 + 3DGS 简介 + 研究动机
2. **関連研究（関連研究）**：NeRF / 3DGS / VGGT / 深度先验类工作
3. **提案手法（提案手法）**：W_geo + W_appear 双权重图分离
4. **実験（実験）**：5 场景 × 多方法 × 4 指标
5. **考察（考察）**：失败案例、局限性、未来工作
6. **結論（おわりに）**

---

## 1. 项目概况

### 1.1 项目目标
将 Meta 的 VGGT（Visual Geometry Grounded Transformer）集成到 3D Gaussian Splatting (3DGS) 训练流程中，通过几何可靠性权重图改进 3DGS 重建质量。

### 1.2 用户身份
- 用户名：GUODEDI
- 邮箱：dguo@borzoi.ai
- 角色：研究生 / 学生
- 工作环境：Windows 10, Miniconda, RTX 3060 12GB
- 项目位置：`d:/Downloads/研究/`

### 1.3 工作目录结构
```
d:/Downloads/研究/
├── vggt/                              # VGGT 代码 + 集成脚本
│   ├── export_vggt_for_3dgs.py        # VGGT 推理 → 输出深度/权重
│   ├── convert_vggt_to_3dgs.py        # VGGT 输出 → 3DGS COLMAP 格式
│   ├── compute_w_appear.py            # W_appear 计算（多视角颜色方差）
│   ├── examples/                      # 测试场景
│   │   ├── kitchen/                   # 厨房（24 张图，用了 5 张）
│   │   ├── llff_fern/                 # 蕨类（20 张，用了 5 张）
│   │   └── llff_flower/               # 花卉（25 张，用了 5 张）
│   └── models/model.pt                # VGGT-1B 权重（4.7 GB）
├── gaussian-splatting-main/           # 3DGS 代码（已加权重图支持）
│   ├── train.py                       # 训练脚本（修改过）
│   ├── render.py                      # 渲染脚本
│   ├── metrics.py                     # 标准评估（test/）
│   └── metrics_train.py               # 自定义评估（train/，逐帧 L1/PSNR/SSIM）
├── output/                            # 训练结果（中文重命名后）
│
├── comparison_visuals/                # 3 方案对比图（GT|Baseline|W_geo）
├── comparison_visuals_full/           # 6 方案完整对比图
├── 渲染对比/                          # 单独渲染图（按方案分文件夹）
├── 点云对比/                          # 所有 .ply 点云（15 个，2.4 GB）
│
├── generate_comparison_visuals.py     # 3 方案对比图生成脚本
├── generate_full_comparison.py        # 6 方案完整对比图生成脚本
├── README.md                          # 英文 README（GitHub 首页）
├── .gitignore
├── 报告_W_geo方案_中文.md             # 中文报告
└── 项目记忆_完整记录.md                # 本文件
```

### 1.4 conda 环境
- **`gaussian_splatting`**：Python 3.10, PyTorch 2.4.0+cu118
- **`vggt`**：Python ?, PyTorch 2.6.0+cu124（用于 VGGT 推理）
- 路径：`D:/miniconda/envs/{env_name}/python.exe`

---

## 2. 核心研究问题与方法

### 2.1 问题（同学的质疑）
> "你们是否把'置信度低的地方'和'Gaussian 难以处理的地方'当成了同一个概念？
> 镜面反射或视角变化导致外观不同，并不一定说明几何位置不正确，
> 因此这里可能存在概念上的问题。"

**确认这个质疑成立**：
- VGGT 低置信度 = 数据质量问题（输入端）
- 3DGS 难建模 = 模型能力问题（输出端，SH 函数表达不了视角依赖外观）
- 反例：镜面（VGGT 准但 3DGS 难）；无纹理墙（VGGT 难但 3DGS 易）

### 2.2 解决方案：权重图分离

**核心创新**：将单一 W 拆为两个语义独立的权重图：

| 权重图 | 加权目标 | 解决问题 |
|--------|---------|---------|
| **W_geo**（已实现并验证） | 深度正则化损失 | VGGT 深度估计的不可靠性 |
| **W_appear**（已实现，待充分验证）| 光度损失 | 3DGS 外观建模能力上限 |

**关键洞察**：两类损失的参考值性质不同
- 深度损失参考 VGGT 估计 → 可能错 → 需要 W_geo 过滤
- 光度损失参考真实照片 → 永远对 → 不该被 W_appear 之外的东西降权

### 2.3 W_geo 公式（最终版）

```
W_geo = clip( max( 0.5·(1-σ) + 0.3·V + 0.2·C , 0.1 ) , 0 , 1 )
```

| 分量 | 含义 | 来源 |
|------|------|------|
| `(1-σ)` | 深度估计可靠性 | VGGT 的 `depth_conf`，5-95 百分位归一化后取反 |
| `V` | 跨帧可见性 | VGGT 轨迹模块 ~2560 个稀疏点，双线性插值至全图 |
| `C` | 多视角几何一致性 | 轨迹点反投影-重投影误差 |

**融合策略**：从乘法改为加权平均（避免值崩塌，<0.2 像素比例从 56% 降至 34%）

---

## 3. 关键代码修改清单

### 3.1 VGGT 侧
- **`vggt/export_vggt_for_3dgs.py`**
  - 第 884 行：consistency 改为 per-pixel（用 `compute_visibility_from_tracks`）
  - 第 908 行：融合策略改为 `weighted_avg`
  - 模型加载加 `mmap=True`（避免 OOM）
  - 内部分辨率改为 518（适配 12GB GPU）
  - 加 `del model` 释放 GPU 内存让轨迹计算能跑

- **`vggt/convert_vggt_to_3dgs.py`**
  - 修复空点云：从深度图反投影生成初始 3D 点（每帧 ~2500 点）
  - 修复 depth_params.json key 格式（去扩展名，用 `image_stem`）
  - 修复 Windows 非 ASCII 路径：`cv2.imdecode/imencode + np.fromfile/tofile`

- **`vggt/compute_w_appear.py`**（新建）
  - 多视角颜色方差计算
  - 加深度一致性校验（过滤虚假方差）

### 3.2 3DGS 侧
- **`gaussian-splatting-main/train.py`**
  - 加双权重图加载逻辑（`weight_map`, `weight_map_appear`）
  - **W_geo 只用于深度损失，不用于光度损失**（关键修正）
  - W_appear 支持三种模式：预计算文件、`online`、`auto`
  - 在线模式：per-pixel EMA loss + 可配置降权策略（top5pct / gentle / extreme / default）
  - auto 模式：warmup 后自动判断"难像素"比例（阈值 20%）

- **`gaussian-splatting-main/scene/cameras.py`**：Camera 加 `weight_map` 和 `weight_map_appear` 字段
- **`gaussian-splatting-main/scene/dataset_readers.py`**：CameraInfo + 加载 .npy
- **`gaussian-splatting-main/utils/camera_utils.py`**：Windows 路径修复（`np.fromfile + cv2.imdecode`）
- **`gaussian-splatting-main/utils/loss_utils.py`**：`weighted_l1_loss`、`weighted_ssim`（修复 size_average=None 分支）
- **`gaussian-splatting-main/arguments/__init__.py`**：加 `weights` 和 `weights_appear` 参数（去掉下划线前缀避免与 `_white_background` 的 `-w` 冲突）
- **`gaussian-splatting-main/scene/__init__.py`**：传递 `weights` 和 `weights_appear`

### 3.3 评估脚本
- **`gaussian-splatting-main/metrics_train.py`**（新建）：从 train/ 目录读渲染图，计算 L1/PSNR/SSIM/LPIPS

---

## 4. 实验结果（最终版）

### 4.1 三场景 W_geo only 结果（7000 iter，metrics 评估）

| 场景 | 方案 | L1 ↓ | PSNR ↑ | SSIM ↑ |
|------|------|------|--------|--------|
| Kitchen | Baseline | 0.00610 | 40.34 | 0.9890 |
| Kitchen | **W_geo** | **0.00599** | **40.40** | 0.9884 |
| Fern | Baseline | 0.01586 | 29.13 | 0.9650 |
| **Fern** | **W_geo** | **0.01320** | **31.05** | **0.9716** |
| Flower | Baseline | 0.04659 | 21.25 | 0.7818 |
| **Flower** | **W_geo** | **0.04177** | **21.75** | **0.8117** |

**改善幅度：**
- Kitchen: L1 -1.8%, PSNR +0.06 dB, SSIM -0.06%（基本持平，已接近上限）
- **Fern: L1 -16.8%, PSNR +1.92 dB, SSIM +0.68%**（最大改善，5/5 帧）
- **Flower: L1 -10.4%, PSNR +0.50 dB, SSIM +3.82%**（5/5 帧）

### 4.2 Kitchen 15000 迭代结果（更长训练）
| Method | L1 | PSNR |
|--------|-----|------|
| Baseline | 0.00541 | 41.31 |
| **W_geo** | **0.00460** | **43.07** (+1.76 dB) |

### 4.3 W_appear 的多种尝试与结果（kitchen/fern/flower）

| 方案 | Kitchen | Fern | Flower |
|------|---------|------|--------|
| Baseline | 38.66 | 27.81 | 21.41 |
| **W_geo only** | **39.23** ✅ | **29.44** ✅ | 21.52 |
| W_geo + W_appear（预计算） | 33.75 ❌ | 26.53 ❌ | **22.02** ✅ |
| W_geo + W_appear（在线 default） | 35.00 | 27.71 | 21.30 |
| W_geo + W_appear（在线 top5pct） | 36.93 | 27.87 | **21.98** |
| Auto + 预计算 | 39.17 | 29.41 | 21.46 |

**W_appear 两种实现方式的本质区别**：

| | **预计算 (Method A)** | **在线 (Method B)** |
|--|--|--|
| 何时算 | 训练前，离线一次性 | 训练中，per-pixel EMA loss |
| 信号 | 真实照片之间的颜色方差（多视角光度一致性）| 训练 loss（模型自我反馈）|
| 输出 | `.npy` 文件 | 内存中的 EMA 张量 |
| 适应性 | 固定 | 随训练动态调整 |
| 类比 | "看历年真题" | "考试中实时调整策略" |
| 物理意义 | 哪里视角依赖外观（镜面）| 哪里 3DGS 持续学不会 |

**结论**：
- W_geo only 是当前最稳定的方案（三场景全部正向）
- W_appear 在 Flower 上有效（+0.55 dB），但在漫反射场景（kitchen/fern）有害
- W_appear 的真正验证需要含镜面/玻璃/高光的场景
- Auto 模式（W_appear 阈值 20%）在三场景判断正确（kitchen/fern DISABLED, flower ENABLED）

### 4.4 关键负面验证
将 W_geo 错误地同时用于光度损失（即原始方案）：
- Kitchen PSNR 38.66 → 32.35（**-6.32 dB**）
- 原因：W_geo 55% 像素 < 0.2，加权光度损失 = 一半像素被压到 1/10 信号
- 这个负面结果验证了同学的质疑

---

## 5. 关键技术决策与教训

### 5.1 经验教训
1. **W_geo 不能加权光度损失** — 真实照片是 ground truth，没有不可靠性需要过滤
2. **乘法融合在分量相关时过度保守** — 加权平均更合适
3. **Windows 日文路径**：`cv2.imread/imwrite` 不工作，必须用 `imdecode/imencode + fromfile/tofile`
4. **3DGS 训练评估** vs **render+metrics**：训练时用 float32，metrics 用 8-bit PNG，结果差 1-2 dB
5. **3DGS 7000 iter 远未收敛**，PSNR 波动大；15000 iter 才稳定
6. **VGGT 1B 模型加载需要 mmap=True**（否则 16GB RAM 不够）
7. **VGGT 推理后必须 `del model`**，否则后续轨迹计算 OOM

### 5.2 用户的偏好
- 喜欢中文沟通和报告
- 偏好详细解释，包括"为什么这么做"
- 经常希望并行跑实验，但 GPU 12GB 内存只能顺序跑（避免 OOM）
- 重要决策前会问 reviewer 视角的问题（"这个数据怎么解读"）
- 报告写作时，喜欢三表对比（baseline / 方案A / 方案B）+ 改善幅度表

---

## 6. 项目当前状态（2026-04-27）

### 6.1 已完成
- ✅ VGGT 推理 → 3DGS 转换流程打通（kitchen/fern/flower 三个场景）
- ✅ W_geo 实现并验证（三场景全部正向）
- ✅ W_appear 实现（预计算 + 在线 + Auto 三种模式）
- ✅ 完整中文报告（`报告_W_geo方案_中文.md`）
- ✅ 日文 PPT 发言稿（5 个幻灯片，N3 级别）
- ✅ GitHub 仓库已转移到 `jr-tagawalab-nttcom/3dgs`（原 `GUODEDI/3dgs-vggt`）
- ✅ 推送权限验证（org transfer 后仍可 push）
- ✅ **可视化对比图全部生成**（老师要求"看到 Visualization"）
  - `comparison_visuals/`：3 方案对比（GT | Baseline | W_geo）
  - `comparison_visuals_full/`：6 方案完整对比（含 W_appear 和 Auto）
  - `渲染对比/`：单独的渲染 PNG 文件按方案分组
  - `点云对比/`：所有 .ply 点云集中（15 个文件，2.4 GB）
- ✅ 误差热力图实现（jet colormap，红=误差大、蓝=误差小，含色阶图例）
- ✅ Kitchen 15000 iter 验证：W_geo 比 Baseline 领先 +1.76 dB（W_geo 真实优势）

### 6.2 GitHub 仓库
- **现地址**：https://github.com/jr-tagawalab-nttcom/3dgs
- **旧地址**：https://github.com/GUODEDI/3dgs-vggt（自动重定向）
- **本地 remote**：已更新为新地址
- **文件数**：274 个文件（已通过 .gitignore 排除大文件）
- **不上传内容**：所有 .md 报告（除 README.md）、output/、*.ply、model.pt、*.npy、压缩包等
- **README**：英文（仓库首页）

### 6.3 可视化展示工具（老师要求 "Visualization"）

**3D 点云查看器**（推荐顺序）：
1. 🥇 **antimatter15.com/splat/** ：拖拽 .ply 即用，**直接支持 3DGS 原始 .ply**
2. 🥈 SuperSplat (`playcanvas.com/supersplat/editor`)：报错"invalid zip"——需要 .splat 格式
3. 🥉 Online 3D Viewer / MeshLab / CloudCompare：只显示点位置，不渲染高斯椭球

**误差热力图**（解读）：
- 红色 = 渲染误差大（render vs gt 像素差大）
- 蓝色 = 渲染误差小
- W_geo 的红色区域明显比 Baseline 少 → 视觉上证明改善

### 6.4 待办 / 未完成
- ⚠️ Fern 和 Flower 用 15000 iter 重跑（可选，让数据更扎实）
- ⚠️ W_appear 在含镜面/玻璃场景的验证（核心未来工作）
- ⚠️ 测试视角（test set）的严格评估（当前是训练视角）
- ⚠️ 完整 30000 iter 训练
- ⚠️ 增加更多场景（fortress, horns, leaves, trex 等 LLFF）

### 6.5 Phase 1 改进（2026-04-27 已大部分完成）

**已完成的代码修改**：

1. **`vggt/export_vggt_for_3dgs.py`**：
   - 加 `--num_frames` 参数（默认 8，原硬编码 5）
   - 用 `np.linspace` 等距采样覆盖整个序列
   - 解决"只用了 21-25% 数据"的问题

2. **`vggt/convert_vggt_to_3dgs.py`**：
   - 加 `--test_indices` 参数（指定测试集帧索引，如 "0,4,7"）
   - 加 `--llffhold` 参数（LLFF 风格的 N 取 1 划分）
   - 自动写 `sparse/0/test.txt`，3DGS 训练时读取

3. **3DGS 默认 llffhold=8**：8 帧训练时第 0 帧自动作为测试集（idx % 8 == 0）

**关键命令更新（带 train/test split）**：
```bash
# 1. VGGT 推理（8 帧）
D:/miniconda/envs/vggt/python.exe -u -X utf8 vggt/export_vggt_for_3dgs.py \
  --scene_dir vggt/examples/<scene> --num_frames 8

# 2. 转换格式（自动 train/test split）
D:/miniconda/envs/gaussian_splatting/python.exe vggt/convert_vggt_to_3dgs.py \
  --vggt_dir vggt/examples/<scene>/vggt --output_dir vggt/examples/<scene>/3dgs_input

# 3. 训练（加 --eval 启用 test 集）
D:/miniconda/envs/gaussian_splatting/python.exe gaussian-splatting-main/train.py \
  -s vggt/examples/<scene>/3dgs_input -d depths --weights weights --eval \
  -m output/<scene>_wgeo_eval --iterations 7000 --test_iterations 7000 \
  --save_iterations 7000 --disable_viewer
```

**已完成 Phase 1 任务**：
- ✅ 重跑 VGGT 推理三场景（8 帧）— 全部完成
- ✅ 重跑 convert 生成 train/test split
- ✅ 训练 baseline + W_geo（带 --eval）
- ✅ 评估 train/test 两套指标
- ✅ W_geo 系数参数化（--w_sigma/--w_vis/--w_cons）
- ✅ 系数消融实验（10 组）— 已完成
- ✅ 创建 recompute_consistency.py（不重跑 VGGT 即可补 cons.npy）
- ✅ metrics_train.py 同时输出 train+test 指标
- ✅ 修融合策略归一化 bug（3 分量加权平均）
- ✅ SIBR_viewers 启动配置（CUDA 12 DLL 复制）
- ✅ 误差热力图可视化报告章节（可视化热力图_报告章节.md）

**Phase 1 剩余任务**：
- ⏳ 修 3 个小 bug（EMA 时机、一致性去重、SSIM 边界）
- ⏳ W_appear 数值稳定性修复（compute_w_appear.py:168）

### 6.6 新增工具脚本（2026-04-27）

| 脚本 | 用途 | 关键参数 |
|------|------|---------|
| `vggt/export_vggt_for_3dgs.py` | VGGT 推理（已升级）| `--num_frames 8 --w_sigma 0.5 --w_vis 0.3 --w_cons 0.2` |
| `vggt/convert_vggt_to_3dgs.py` | 转 3DGS 格式（已升级）| `--llffhold 8`（自动 train/test split）|
| `vggt/recompute_wgeo.py` | **轻量重算 W.npy**（消融用）| `--w_sigma X --w_vis Y --w_cons Z` |
| `vggt/recompute_consistency.py` | **从已有数据补 cons.npy** | `--vggt_dir <path>` |
| `gaussian-splatting-main/metrics_train.py` | **train+test 双评估** | `-m <model_path>` |
| `run_ablation.py` | **W_geo 系数消融自动化**（10 组）| 直接运行 |

### 6.7 SIBR Viewer 配置笔记

**问题**：`cudart64_12.dll missing` 错误
**原因**：SIBR 预编译版用 CUDA 12，本机 CUDA 11.8
**解决**：从 vggt 环境复制 CUDA 12 DLL 到 viewers/bin/

```bash
cp D:/miniconda/envs/vggt/Library/bin/cudart64_12.dll D:/Downloads/研究/viewers/bin/
cp D:/miniconda/envs/vggt/Library/bin/cublas64_12.dll D:/Downloads/研究/viewers/bin/
cp D:/miniconda/envs/vggt/Library/bin/cublasLt64_12.dll D:/Downloads/研究/viewers/bin/
cp D:/miniconda/envs/vggt/Library/bin/cusparse64_12.dll D:/Downloads/研究/viewers/bin/
```

**SIBR 启动命令**：
```bash
cd d:/Downloads/研究/viewers/bin
./SIBR_gaussianViewer_app.exe -m "d:/Downloads/研究/output/<scene_name>"
```

**找训练相机视角**：界面左侧 "Camera Point view" 面板 → `Snap to closest` 按钮（一键贴最近相机）或 `Snap to` 输入 0~7 切换具体相机。

### 6.6 评审反馈关键点

**老师/同学反馈记录**：
1. **同学的概念质疑**（已用实验回应）：
   > "你们是否把'置信度低的地方'和'Gaussian 难以处理的地方'当成了同一个概念？"
   - 已通过双权重图架构 + 实验数据回答

2. **老师对"定量数据"的要求**（2026-04 中）：
   - 已补充 L1/PSNR/SSIM 三指标 + 逐帧详细数据

3. **老师对"Visualization 可视化"的要求**（2026-04-27）：
   - 已生成 `comparison_visuals/` 三联拼图 + 误差热力图
   - 已生成 `comparison_visuals_full/` 6 方案完整对比
   - 已整理 `渲染对比/` 单独 PNG + `点云对比/` 全部 .ply

4. **学术严谨性的痛点（用户自己提出的）**：
   - 硬编码常数太多 → Phase 1.2 处理
   - W_appear 始终拿不到正向结果 → Phase 2 需要镜面场景
   - 无 train/test split → Phase 1.1 处理（**正在进行**）

---

## 7. 关键文件路径快查

### 7.1 报告类
- `d:/Downloads/研究/报告_W_geo方案_中文.md`
- `d:/Downloads/研究/vggt/PPT発言稿_全ページ_日文N3_中文对照.md`
- `d:/Downloads/研究/vggt/PPT発言稿_第4页_日文N3_中文对照.md`（反例页）
- `d:/Downloads/研究/vggt/PPT発言稿_摘要_日文N3_中文对照.md`
- `d:/Downloads/研究/vggt/PPT発言稿_改善方向スライド_日文N3_中文对照.md`
- `d:/Downloads/研究/vggt/权重图W的语义分析与改进方向.md`
- `d:/Downloads/研究/vggt/具体改进方案_多视角光度一致性.md`

### 7.2 实验输出（output/）
所有训练结果按"场景_方案_PSNR"命名：

**核心 3 场景 × 6 方案**：
- `厨房_无权重_基线_38.66dB`
- `厨房_W几何_深度加权_39.23dB`           ← W_geo only
- `厨房_W几何+W外观_预计算_33.75dB`       ← Method A
- `厨房_W几何+W外观_在线_35.00dB`         ← Method B
- `厨房_自动模式_最终_39.17dB`            ← Auto
- `厨房_在线top5pct_36.93dB`              ← Method B 不同策略

（Fern 和 Flower 同样命名规则）

**已渲染**（`train/ours_7000/renders/` 和 `gt/`）：
- 3 场景 × baseline + W_geo（6 个）
- 3 场景 × Wgeo+Wappear（预计算）+ Wgeo+Wappear（在线）+ Auto（9 个）
- 共 15 个模型已渲染

### 7.3 可视化输出文件夹

| 文件夹 | 内容 | 用途 |
|--------|------|------|
| `comparison_visuals/` | 3 方案对比拼图（GT | Baseline | W_geo + 误差热力图 + 色阶图例）| 报告主图 |
| `comparison_visuals_full/` | 6 方案完整对比（GT + 5 个方法 + 误差热力图）| 进阶分析 |
| `渲染对比/` | 单独 PNG 按方案分组（真实照片/Baseline渲染/Wgeo渲染）| PPT 局部放大 |
| `点云对比/` | 15 个 .ply 文件集中存放 | 3D viewer 加载 |

### 7.4 关键命令
```bash
# 激活环境
conda activate gaussian_splatting

# VGGT 推理（vggt 环境）
D:/miniconda/envs/vggt/python.exe -u -X utf8 vggt/export_vggt_for_3dgs.py --scene_dir vggt/examples/<scene>

# 转换格式
D:/miniconda/envs/gaussian_splatting/python.exe vggt/convert_vggt_to_3dgs.py \
  --vggt_dir vggt/examples/<scene>/vggt --output_dir vggt/examples/<scene>/3dgs_input

# 训练（W_geo only）
D:/miniconda/envs/gaussian_splatting/python.exe gaussian-splatting-main/train.py \
  -s vggt/examples/<scene>/3dgs_input -d depths --weights weights \
  -m output/<scene>_wgeo --iterations 7000 --test_iterations 7000 \
  --save_iterations 7000 --disable_viewer

# 渲染 + 评估
cd gaussian-splatting-main
D:/miniconda/envs/gaussian_splatting/python.exe render.py -m <model_path> --iteration 7000
D:/miniconda/envs/gaussian_splatting/python.exe metrics_train.py -m <model_path>
```

---

## 8. 报告核心论点（一页式总结）

> 本研究针对 VGGT + 3DGS 集成中"VGGT 低置信度"与"3DGS 难建模"两个不同概念被混用的问题，
> 提出将单一权重图 W 拆分为两个语义独立的权重图：
>
> - **W_geo**（几何可靠性）：解决 VGGT 深度估计可能出错的问题，加权深度正则化损失
> - **W_appear**（外观可建模性）：解决 3DGS SH 函数表达能力上限的问题，加权光度损失
>
> 关键发现：
> 1. 原方案将 W 同时作用于光度损失会导致 PSNR 暴跌 -6.32 dB（验证了"概念混淆"的实质危害）
> 2. W_geo 单独使用在三场景上全部正向（Kitchen +0.06, Fern +1.92, Flower +0.50 dB PSNR）
> 3. W_appear 的有效性依赖场景特征，需要含镜面/高光的场景充分验证
>
> 创新点：
> - 概念辨析（数据质量 vs 模型能力）
> - 双权重图架构
> - 融合策略修正（乘法 → 加权平均）
> - Auto 模式自动判断是否启用 W_appear
> - 完整工程实现 + 三场景实验验证

---

*文档版本：v1.0*
*生成日期：2026-04-26*
*用途：跨会话 / 跨模型工作交接*
